環境構築
```bash
npm create vite
```
プロジェクト名を書く
質問に答えていく
```bash
Select a framework: > React
Select a variant: > TypeScript + SWC

```
dnd-kitインストール
```bash
npm install @dnd-kit/core @dnd-kit/sortable
```